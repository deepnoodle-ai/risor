#!/usr/bin/env risor --

function incrementer(value=0) {
    function inner() {
        let result = value
        value++
        return result
    }
    return inner
}

let inc = incrementer()
print(inc())
print(inc())
print(inc())
print(inc())
