import sched
import time

let once = sched.once("1s", function(){
  print("once")
})

let cron = sched.cron("*/1 * * * * *", function(){
  print("hola")
})
print(cron.is_running())

let every = sched.every("1m", function() {
  print("every 1 minute")
})

for {
  time.sleep(1)
}
