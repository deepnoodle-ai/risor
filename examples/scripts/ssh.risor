
// Connect to server
let client = ssh(getenv("USER"), getenv("HOST"), {
    password: getenv("PASSWORD"),
    port: 22,
    timeout: 30
})

let result = client.execute("uname -a")
print("System info:", result)

client.close()
