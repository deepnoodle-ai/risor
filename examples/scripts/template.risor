#!/usr/bin/env risor --

let data = fetch("http://ipinfo.io").json()

let tpl = template.new("test")
tpl.delims("{%", "%}")

# named templates
# tpl.add("ipinfo", "You are in {% .city %}, region {% .region %} in {% .timezone %}")
# let res = tpl.execute_template(data, "ipinfo")

tpl.parse("You are in {% .city %}, region {% .region %} in {% .timezone %}")
let res = tpl.execute(data)

print(res)
